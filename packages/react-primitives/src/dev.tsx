import React from "react";
import ReactDOM from "react-dom/client";

import "./dev.css";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <div>UI primitives</div>
  </React.StrictMode>
);
