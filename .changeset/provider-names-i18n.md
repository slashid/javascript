---
"@slashid/react": minor
---

Add `provider.*` text keys so OIDC and SAML provider display names can be localized. The authenticating screen title now shows the same name as the initial screen button, including the factor `label`. The default name for `azuread` is now "Microsoft".
